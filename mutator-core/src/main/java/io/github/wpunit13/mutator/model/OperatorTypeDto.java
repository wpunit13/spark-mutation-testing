package io.github.wpunit13.mutator.model;

/**
 * Mutator-core-owned enum mirror of api.OperatorType; no catalyst import.
 */
public enum OperatorTypeDto {
    JOIN,
    FILTER,
    AGGREGATE,
    WINDOW,
    PROJECT,
    OTHER
}
